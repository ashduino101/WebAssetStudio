/* tslint:disable */
/* eslint-disable */
/**
* @param {Uint8Array} data
*/
export function swap_bytes_xbox(data: Uint8Array): void;
/**
* @param {Uint8Array} data
*/
export function bgr2rgb(data: Uint8Array): void;
/**
* @param {Uint8Array} data
* @returns {Uint8Array}
*/
export function decode_a8(data: Uint8Array): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_argb4444(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgb24(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_argb32(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgb565(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bgr565(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_r16(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgba4444(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bgra4444(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgba5551(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bgra5551(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgba1010102(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bgra32(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rhalf(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rghalf(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgbhalf(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgbahalf(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rfloat(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgfloat(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgbfloat(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgbafloat(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_yuy2(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgb9e5float(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rg16(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_r8(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_l8(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_la16(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rg32(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgb48(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_rgba64(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_dxt1(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_dxt3(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_dxt5(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @param {boolean} is2bpp
* @returns {Uint8Array}
*/
export function decode_pvrtc(data: Uint8Array, width: number, height: number, is2bpp: boolean): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_etc1(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_etc2(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_etc2_a1(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_etc2_a8(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_eacr(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_eacr_signed(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_eacrg(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_eacrg_signed(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bc4(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bc5(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bc6h(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_bc7(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_atc_rgb4(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @returns {Uint8Array}
*/
export function decode_atc_rgba8(data: Uint8Array, width: number, height: number): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @param {number} block_width
* @param {number} block_height
* @returns {Uint8Array}
*/
export function decode_astc(data: Uint8Array, width: number, height: number, block_width: number, block_height: number): Uint8Array;
/**
* Decodes a texture of arbitrary format.
* Crunched textures must be unpacked before decoding.
*
* # Arguments
*
* * `format` - A string holding the texture format (e.g. "RGBA32")
* * `data` - An array of bytes holding the compressed image data to decode
* * `width` - The overall width of the image
* * `height` - The overall height of the image
* * `is_xbox` - If the platform is XBox 360 -- used to determine if bytes should be swapped
*
* # Returns
*
* * A box containing the decompressed (raw) image data.
* @param {string} format
* @param {Uint8Array} data
* @param {number} width
* @param {number} height
* @param {boolean} is_xbox
* @returns {Uint8Array}
*/
export function decode(format: string, data: Uint8Array, width: number, height: number, is_xbox: boolean): Uint8Array;
/**
* @param {Uint8Array} data
* @returns {Uint8Array}
*/
export function lz4_compress(data: Uint8Array): Uint8Array;
/**
* @param {Uint8Array} data
* @param {number} out_size
* @returns {Uint8Array}
*/
export function lz4_decompress(data: Uint8Array, out_size: number): Uint8Array;
/**
* @param {number} width
* @param {number} height
* @param {Uint8Array} data
* @param {boolean} flip
* @returns {Uint8Array}
*/
export function encode_png(width: number, height: number, data: Uint8Array, flip: boolean): Uint8Array;
/**
* Handler for `console.log` invocations.
*
* If a test is currently running it takes the `args` array and stringifies
* it and appends it to the current output of the test. Otherwise it passes
* the arguments to the original `console.log` function, psased as
* `original`.
* @param {Array<any>} args
*/
export function __wbgtest_console_log(args: Array<any>): void;
/**
* Handler for `console.debug` invocations. See above.
* @param {Array<any>} args
*/
export function __wbgtest_console_debug(args: Array<any>): void;
/**
* Handler for `console.info` invocations. See above.
* @param {Array<any>} args
*/
export function __wbgtest_console_info(args: Array<any>): void;
/**
* Handler for `console.warn` invocations. See above.
* @param {Array<any>} args
*/
export function __wbgtest_console_warn(args: Array<any>): void;
/**
* Handler for `console.error` invocations. See above.
* @param {Array<any>} args
*/
export function __wbgtest_console_error(args: Array<any>): void;
/**
* Runtime test harness support instantiated in JS.
*
* The node.js entry script instantiates a `Context` here which is used to
* drive test execution.
*/
export class WasmBindgenTestContext {
  free(): void;
/**
* Creates a new context ready to run tests.
*
* A `Context` is the main structure through which test execution is
* coordinated, and this will collect output and results for all executed
* tests.
*/
  constructor();
/**
* Inform this context about runtime arguments passed to the test
* harness.
*
* Eventually this will be used to support flags, but for now it's just
* used to support test filters.
* @param {any[]} args
*/
  args(args: any[]): void;
/**
* Executes a list of tests, returning a promise representing their
* eventual completion.
*
* This is the main entry point for executing tests. All the tests passed
* in are the JS `Function` object that was plucked off the
* `WebAssembly.Instance` exports list.
*
* The promise returned resolves to either `true` if all tests passed or
* `false` if at least one test failed.
* @param {any[]} tests
* @returns {Promise<any>}
*/
  run(tests: any[]): Promise<any>;
}
